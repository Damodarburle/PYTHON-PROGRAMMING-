def right_pyramid_pattern(n):
    for i in range(1, n + 1):
        # Print leading spaces
        print(" " * (n - i), end="")
        # Print stars
        print("*" * i)

# Example usage
n = int(input("Enter the number of rows for the pyramid: "))
right_pyramid_pattern(n)
